<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">
    <changeSet id="create-staff-projects-table" author="gizbrekht.md">
        <sql>
            CREATE TABLE public.staff_projects (
                order_id int8 NOT NULL,
                staff_id int8 NOT NULL,
                CONSTRAINT staff_projects_orders_fk FOREIGN KEY (order_id) REFERENCES public.orders(id),
                CONSTRAINT staff_projects_users_fk FOREIGN KEY (staff_id) REFERENCES public.staff(id) ON UPDATE CASCADE
            );
        </sql>
    </changeSet>
</databaseChangeLog>